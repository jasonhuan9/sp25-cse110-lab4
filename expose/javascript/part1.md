1. It prints "values added: 20"
2. It prints "values added: 20"
3. You shouldn't use var because its a function scope and that means it can be accessed from anywhere inside the function its definined in.
4. Prints "values added: 20"
5. The code returns an error because we define result using let instead of var and we are trying to access it from outside the if statement.
6. It returns an error because result is now a constant variable and then we are trying change the value of it.
7. It gives us an error because we are trying to access result outside the if statement. Only var can be accessed from anywhere in the function.