1. It will print 3 because in the for loop it will interate until it equals prices.length which is 3, so it will print 3.
2. It will print 150 because thats the last calculated number.
3. It will print 150 because thats the last value updated to finalPrice.
4. It will return 50, 100, 150 because the function halfs all the prices.
5. It will give an error because i isn't defined. Its only recoginzed in the for loop.
6. It will give an error because discounted price is only recogized in the for loop. Its not accessible outside of it.
7. It will print 150 because the "let finalPrice" is defined in the functiont this time instead of just in the for loop.
8. It will return 50, 100, 150 because the function is written properly and will half the original prices.
9. It will give an error because the i is only accessable inside the function.
10. It will print the prices length which is 3 because the length variable is equal to prices.length which is 3.
11. It returns 50, 100, 150. In the function it goes through each price and halfs them then adds it to the discounted array.
12. A. student.name B. student['Grad Year'] C. student.greeting() D. student['Favorite Teacher'].name E. student.courseLoad[0]
13. A. '32', 2 is converted to a string B. 1, the 3 becomes a number C. 3, null becomes 0 D. '3null', null becomes a string E. 4, true becomes 1 F. 0, false and null become 0 G. '3undefined', undefined becomes a string H. NaN, undefined becomes NaN
14. A. true, '2' becomes 2 B. false, lexicographical comparison '2' is greater than '12' C. true, '2' becomes 2 D. false, strict equality, string and number aren't the same E. false, true becomes 1 F. true, boolean(2) = true
15. === is a strict equality operator that checks the equality without type conversion while == has type conversion.
17. The result will be [2,4,6] because the modify array function takes an array and another function, and pushes the new values from the do somthing function, it multiplies the values by 2, and thats how we get the result of [2,4,6].
19. The output is 1, 4, 3, 2.